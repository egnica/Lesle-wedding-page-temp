version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "No preBuild step for static site"
    build:
      commands:
        - echo "No build step for static site"
  artifacts:
    baseDirectory: .
    files:
      - "**/*"
  cache:
    paths: []
